import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mdrrmojaen.app',
  appName: 'DRR App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
